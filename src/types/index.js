const { equal, notEqual } = require("./var.type");
const { shouldContain, shouldNotContain } = require("./program.type");

module.exports = {
  equal,
  notEqual,
  shouldContain,
  shouldNotContain,
};
